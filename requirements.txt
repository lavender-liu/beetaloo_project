# Core geospatial
laspy[lazrs]>=2.5
rasterio>=1.3
pyproj>=3.6
shapely>=2.0
geopandas>=0.14
numpy>=1.26
scipy>=1.12
pandas>=2.1
pyarrow>=14.0

# LiDAR
# pdal  # optional, for CSF ground classification if needed

# Remote sensing — SAR (Planetary Computer, free, no credentials)
planetary-computer>=1.0
pystac-client>=0.7,<0.9

# Remote sensing — Sentinel-2 / DEA
odc-stac>=0.3.9
odc-algo>=0.2.3
pystac-client>=0.7,<0.9

# Xarray / Zarr
xarray>=2024.1
zarr>=2.16,<3.0
dask>=2024.1

# Modelling
scikit-learn>=1.4,<1.6
quantile-forest>=1.3.3
joblib>=1.3

# Visualisation / notebooks
matplotlib>=3.8
jupyter>=1.0
ipykernel>=6.29